# تشغيل النظام المجاني على Firebase

هذا الإصدار لا يستخدم Cloud Functions ولا يحتاج خطة Blaze أو بطاقة دفع.

## 1) Authentication

من Firebase Console:

1. افتح **Authentication → Sign-in method**.
2. فعّل **Anonymous** فقط لكي يستطيع الموقع الاتصال بقاعدة البيانات.
3. لا يحتاج المستخدمون إلى حسابات Email/Password؛ حساباتهم يديرها البرنامج داخل Realtime Database.

## 2) Realtime Database

1. افتح **Build → Realtime Database**.
2. أنشئ القاعدة إذا لم تكن موجودة.
3. افتح تبويب **Rules**.
4. الصق محتوى ملف `database.rules.json` واضغط **Publish**.

## 3) الملفات

ارفع إلى GitHub Pages مع الحفاظ على نفس ترتيب المجلدات:

```text
index.html
fonts/SKR-HEAD1.woff
```

الخط يحتوي أيضًا على رابط إنترنت احتياطي، لكن رفعه داخل المستودع يجعل تشغيله أكثر ثباتًا.

يمكن رفع أي خطوط إضافية مباشرة داخل مجلد `fonts` بصيغ `woff2` أو `woff` أو `ttf` أو `otf`. يقرأ البرنامج محتويات المجلد تلقائيًا من GitHub ويضيف الخطوط إلى الإعدادات والمعاينة والطباعة دون تعديل `index.html`. اسم الخط الظاهر يُؤخذ من اسم الملف؛ لذلك استخدم اسمًا واضحًا مثل `My-Arabic-Font.woff2`.

ملفات الإدارة المحلية:

```text
firebase.json
database.rules.json
.firebaserc
```

## 4) تسجيل الدخول

أول دخول:

```text
اسم المستخدم: admin
كلمة المرور: 123456
```

غيّر كلمة مرور admin من **الإعدادات → الأمان**.

## 5) نظام المستخدمين

- المستخدمون محفوظون في `appUsers` بكلمات مرور مجزأة SHA-256.
- أسماء المستخدمين العاديين تُكتب بالعربية فقط، والاسم نفسه يُستخدم في الدخول ويظهر داخل النظام.
- كلمة مرور المستخدم العادي تتكون من 3 أرقام فقط. كلمة مرور admin تظل مستقلة ويمكن أن تكون أطول.
- حساب المدير فقط يظل باسم `admin`.
- بيانات كل مستخدم محفوظة في `userData/{username}`.
- الإعدادات المشتركة محفوظة في `shared/settings`.
- الاتصال الحالي محفوظ في `presence`.
- الطرد يتم من خلال `control`.
- الإعدادات تفتح للأدمن فقط، ويُطلب باسورد الأدمن عند أول فتح للإعدادات في الجلسة فقط؛ لا يُطلب مرة أخرى إلا بعد تسجيل الخروج أو انتهاء جلسة 10 ساعات.
- الخط الافتراضي للكود واللون والمقاس هو Arial، ويعمل في المعاينة والطباعة على أجهزة Windows.
- خطا Arial وSKR HEAD1 موجودان دائمًا في قائمة الخطوط، وباقي خطوط Windows تظهر بعد منح صلاحية قراءة خطوط الجهاز.

## ملاحظة أمنية مهمة

هذا النظام مجاني ويعمل بالكامل داخل المتصفح مثل نظام jard، ولذلك الحماية إدارية داخل واجهة البرنامج وليست مساوية لحماية Firebase Admin SDK. أي شخص تقني يمتلك صلاحية Anonymous وقواعد القراءة والكتابة قد يستطيع التعامل مباشرة مع قاعدة البيانات. النظام مناسب للاستخدام الداخلي الموثوق، أما الحماية القوية الكاملة فتحتاج Backend موثوقًا مثل Cloud Functions.
