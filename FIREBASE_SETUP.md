# خطوات تشغيل البرنامج مع Firebase

## 1) تفعيل تسجيل الدخول

من Firebase Console:

1. افتح **Authentication**.
2. اضغط **Get started**.
3. افتح **Sign-in method**.
4. فعّل **Email/Password** ثم احفظ.

عند أول تشغيل، سجّل الدخول بالبيانات الافتراضية:

- اسم المستخدم: `admin`
- كلمة المرور: `123456`

إذا لم يكن حساب المدير موجودًا، ينشئه البرنامج تلقائيًا عند أول دخول ناجح. يمكن تغيير كلمة المرور لاحقًا من **الإعدادات → الأمان**.

## 2) إنشاء Cloud Firestore

1. افتح **Firestore Database**.
2. اضغط **Create database**.
3. اختر موقع قاعدة البيانات المناسب.
4. أكمل إنشاء القاعدة.

> البرنامج يستخدم Cloud Firestore، وليس Realtime Database.

## 3) تثبيت قواعد الأمان

افتح **Firestore Database → Rules**، واستبدل القواعد بمحتوى ملف `firestore.rules`، ثم اضغط **Publish**.

## 4) رفع الموقع على GitHub Pages

ارفع `index.html` إلى المستودع، ثم فعّل GitHub Pages من **Settings → Pages**.

## ملاحظات

- البيانات والإعدادات محفوظة على Firestore وليست في localStorage.
- يلزم اتصال بالإنترنت لتسجيل الدخول وتحميل البيانات.
- اختيار اسم خط Windows ينتقل كإعداد، لكن شكل الخط لن يتطابق على جهاز لا يحتوي على الخط؛ عندها يستخدم البرنامج Cairo ثم Arial.
